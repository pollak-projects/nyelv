import { reactive } from "vue";


export const user_current_progress_store = reactive({
  user_current_progress : 0
});

export const user_current_level_store = reactive({
  user_current_progress : 0
});